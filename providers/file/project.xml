<?xml version="1.0"?>
<project xmlns="http://maven.apache.org/POM/3.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/3.0.0 http://maven.apache.org/maven-v3_0_0.xsd">
    <extend>${basedir}/../project.xml</extend>
    <pomVersion>3</pomVersion>
    <artifactId>mule-file-provider</artifactId>
    <name>Mule File Provider</name>
    <description>A Mule provider for File Connectivity. This connector allows files to be read and written to
        directories on the local file system. The connector can be configured to filter the file it reads and the way
        files are written, such as whether binary output is used or the file is appended to.
    </description>
    <shortDescription>file</shortDescription>
    <url>http://www.muleumo.org/docs/modules/mule-file-provider/</url>
    <dependencies/>
</project>
