/*
 * Generated by the Mule Catalog Archetype. http://mule.mulesoft.org
 */

package ${patternBuilderPackageName};

import org.mule.exception.DefaultServiceExceptionStrategy;
import org.mule.tck.junit4.AbstractMuleTestCase;

import ${patternFQCN};

import static org.junit.Assert.assertEquals;

public class ${patternBuilderClassName}TestCase extends AbstractMuleTestCase
{
    public void testFullConfiguration() throws Exception
    {
        final String patternName = "${patternTagName}-full-test";

        // TODO call any extra method on the builder
        final ${patternClassName} ${patternVariableName} = new ${patternBuilderClassName}()
        	.name(patternName)
#if($patternType == "ms")
            .messageSource(getTriggerableMessageSource())
#end
#if($patternType == "si" || $patternType == "siso")
            .inboundAddress("test://foo.in")
#end
#if($patternType == "siso")
            .outboundAddress("test://foo.out")
#end
        	.exceptionStrategy(new DefaultServiceExceptionStrategy(muleContext))
        	.build(muleContext);

        assertEquals(patternName, ${patternVariableName}.getName());
    }
}
