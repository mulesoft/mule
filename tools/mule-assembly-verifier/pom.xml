<?xml version="1.0" encoding="UTF-8"?>

<!-- $Id$ -->

<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>org.mule.tools</groupId>
    <artifactId>mule-assembly-verifier</artifactId>
    <version>1.1</version>
    <packaging>maven-plugin</packaging>
    <name>Mule Assembly Verifier Plugin</name>

    <description>
        Verify contents of the output assembly against a controlled set of libraries.
    </description>

    <properties>
        <muleVersion>2.0.0-M3-SNAPSHOT</muleVersion>
        <groovyMojoVersion>1.0-beta-1</groovyMojoVersion>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.apache.maven</groupId>
            <artifactId>maven-project</artifactId>
            <!-- TODO inherit top-level pre-reqs for Maven  -->
            <version>2.0.6</version>
        </dependency>
        <dependency>
            <groupId>org.codehaus.mojo.groovy</groupId>
            <artifactId>groovy-mojo-support</artifactId>
            <version>${groovyMojoVersion}</version>
        </dependency>
    </dependencies>

    <build>
        <defaultGoal>install</defaultGoal>

        <!-- 
             WebDAV for remote deployment (necessary as this pom does not have the
             toplevel pom as parent)
        -->
        <extensions>
            <extension>
                <groupId>org.apache.maven.wagon</groupId>
                <artifactId>wagon-webdav</artifactId>
            </extension>
        </extensions>

        <plugins>
            <plugin>
                <groupId>org.codehaus.mojo.groovy</groupId>
                <artifactId>groovy-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>compile</goal>
                        </goals>
                        <configuration>
                            <sources>
                                <!--
                                HACK: The groovy:compile goal normally finds sources under src/main/groovy,
                                      but the archetype plugin can only put Java-like sources into src/main/java.
                                -->
                                <fileset>
                                    <directory>${pom.basedir}/src/main/java</directory>
                                    <includes>
                                        <include>**/*.groovy</include>
                                    </includes>
                                </fileset>
                            </sources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-plugin-plugin</artifactId>
                <dependencies>
                    <dependency>
                        <groupId>org.codehaus.mojo.groovy</groupId>
                        <artifactId>groovy-mojo-tools</artifactId>
                        <version>${groovyMojoVersion}</version>
                    </dependency>
                </dependencies>
            </plugin>


        </plugins>
    </build>

    <distributionManagement>
        <downloadUrl>http://mule.mulesource.org/display/MULE/Download</downloadUrl>
        <repository>
            <id>mule-releases</id>
            <name>Mule Release Repository</name>
            <url>dav:https://dav.codehaus.org/repository/mule/</url>
        </repository>
        <snapshotRepository>
            <id>mule-snapshots</id>
            <name>Mule Snapshot Repository</name>
            <url>dav:https://dav.codehaus.org/snapshots.repository/mule/</url>
            <uniqueVersion>false</uniqueVersion>
        </snapshotRepository>
        <site>
            <id>mule-site</id>
            <name>Mule Website</name>
            <!-- TODO just to make CI server happy, should be something else -->
            <url>dav:https://dav.codehaus.org/mule/docs/${muleVersion}/mule-tools/mule-assembly-verifier</url>
        </site>
    </distributionManagement>

    <profiles>
        <profile>
            <id>ci-server</id>
            <distributionManagement>
                <site>
                    <!-- TODO just to make CI server happy, should be something else -->
                    <url>file:///home/luntbuild/publish/mule/docs/${muleVersion}/mule-tools/mule-assembly-verifier</url>
                </site>
            </distributionManagement>
        </profile>
    </profiles>

    <!-- TODO section generated by the archetype, not necessarily required  -->
    <!-- reporting>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-report-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jxr-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <configuration>
                    <links>
                        <link>http://java.sun.com/j2ee/1.4/docs/api</link>
                        <link>http://java.sun.com/j2se/1.5.0/docs/api</link>
                    </links>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-plugin-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-project-info-reports-plugin</artifactId>
            </plugin>
        </plugins>
    </reporting -->

</project>
