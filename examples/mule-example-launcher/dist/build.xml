<!--
 Copyright (c) MuleSoft, Inc.  All rights reserved.  http://www.mulesoft.com

 The software in this package is published under the terms of the CPAL v1.0
 license, a copy of which has been included with this distribution in the
 LICENSE.txt file.
-->
<project name="Mule Example Launcher" default="deploy">

    <property name="app.name" value="mule-example-launcher"/>
    <property environment="env"/>

    <property name="dir.java" value="src"/>
    <property name="dir.conf" value="conf"/>
    <property name="dir.build" value="target"/>
    <property name="dir.classes" value="${dir.build}/classes"/>

    <property name="url.maven.repo" value="http://repo1.maven.org/maven2"/>
    
    <target name="clean"
            description="Clean the output directory (start from scratch).">
        <delete dir="${dir.build}"/>
    </target>

    <!-- Download any additional libraries needed to run the application -->
    <target name="get.libraries">
        <get src="${url.maven.repo}/xmlunit/xmlunit/1.1/xmlunit-1.1.jar"
             dest="${dir.user.lib}/xmlunit-1.1.jar"
             verbose="true"
             usetimestamp="true"/>
    </target>

    <target name="init" depends="set-paths,get.libraries">
        <!-- Create the build directory if it doesn't exist -->
        <mkdir dir="${dir.classes}"/>
        
        <!-- Configure Mule classpath (mirrors wrapper.conf settings) -->
        <path id="classpath.mule">
            <pathelement location="${dir.mule.home}/conf"/>
            <fileset dir="${dir.mule.home}/lib/user">
                <include name="**/*.jar"/>
            </fileset>
            <fileset dir="${dir.mule.home}/lib/mule">
                <include name="**/*.jar"/>
            </fileset>
            <fileset dir="${dir.mule.home}/lib/opt">
                <include name="**/*.jar"/>
            </fileset>
            <fileset dir="${dir.mule.home}/lib">
                <include name="**/*.jar"/>
            </fileset>
        </path>
    </target>

    <target name="compile"
            depends="init"
            description="Compile the application.">

        <javac srcdir="${dir.java}"
               destdir="${dir.classes}"
               debug="true"
               source="1.5"
               target="1.5">
            <classpath refid="classpath.mule"/>
        </javac>

    </target>

   <target name="package" 
           depends="compile" 
           description="Package the application">
           
        <jar jarfile="${dir.build}/${app.name}.jar" 
             basedir="${dir.classes}"/>
   	
    <zip destfile="${app.name}.zip">
        <zipfileset file="${dir.build}/${app.name}.jar" fullpath="lib/${app.name}.jar"/>
        <zipfileset dir="src/main/app" includes="docroot/**,mule-config.xml"/>            
    </zip>
   	
   </target>

   <target name="deploy" 
           depends="package" 
           description="Deploy the application to the Mule server">   	
        <copy file="${app.name}.zip" todir="${dir.mule.home}/apps" overwrite="true"/>
   </target>

    <target name="set-paths">
        <property name="dir.mule.home" value="${basedir}/../.."/>
        <property name="dir.user.lib" value="${dir.mule.home}/lib/user/"/>
    </target>
</project>
