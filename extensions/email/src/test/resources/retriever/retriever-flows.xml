<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns:email="http://www.mulesoft.org/schema/mule/email"
      xmlns:spring="http://www.springframework.org/schema/beans" xmlns="http://www.mulesoft.org/schema/mule/core"
      xsi:schemaLocation="http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
               http://www.mulesoft.org/schema/mule/email http://www.mulesoft.org/schema/mule/email/current/mule-email.xsd">

    <flow name="retrieveAndRead">
        <email:list config-ref="config"/>
    </flow>

    <flow name="retrieveMatchingSubjectAndFromAddress">
        <email:list config-ref="config">
            <email:matcher subjectRegex="Email Subject" fromRegex="@mulesoft"/>
        </email:list>
    </flow>

    <flow name="retrieveWithAttachments">
        <email:list config-ref="config"/>
    </flow>

    <flow name="storeMessages">
        <email:list config-ref="config"/>
        <flow-ref name="email-to-id"/>
        <email:store config-ref="config" localDirectory="${storePath}"/>
    </flow>

    <flow name="storeSingleMessage">
        <email:store config-ref="config" localDirectory="${storePath}">
            <email:email-ids>
                <email:email-id value="1"/>
            </email:email-ids>
        </email:store>
    </flow>

    <flow name="retrieveAndDelete">
        <email:list config-ref="config"/>
        <flow-ref name="email-to-id"/>
        <email:delete config-ref="config"/>
    </flow>

    <flow name="retrieveAndDeleteIncomingAndScheduled">
        <email:list config-ref="config"/>
        <flow-ref name="email-to-id"/>
        <email:delete config-ref="config">
            <email:email-ids>
                <email:email-id value="6"/>
            </email:email-ids>
        </email:delete>
    </flow>

    <sub-flow name="email-to-id">
        <set-variable variableName="ids" value="#[[]]"/>
        <foreach collection="#[payload]" counterVariableName="email">
            <expression-transformer expression="#[flowVars.ids.add(payload.attributes.id)]"/>
        </foreach>
        <set-payload value="#[flowVars.ids]"/>
    </sub-flow>

</mule>