<?xml version="1.0" encoding="UTF-8"?>

<mule xmlns:http="http://www.mulesoft.org/schema/mule/http" xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:test="http://www.mulesoft.org/schema/mule/test"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
      http://www.mulesoft.org/schema/mule/test http://www.mulesoft.org/schema/mule/test/current/mule-test.xsd
      http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd">

    <http:listener-config name="listenerConfig" host="localhost" port="${port1}" usePersistentConnections="false" />

    <flow name="200Flow">
        <http:listener config-ref="listenerConfig" path="200">
            <http:response-builder statusCode="200"/>
        </http:listener>
        <set-payload value="ok" />
    </flow>

    <flow name="201Flow">
        <http:listener config-ref="listenerConfig" path="201">
            <http:response-builder statusCode="201"/>
        </http:listener>
        <set-payload value="ok" />
    </flow>

    <flow name="202Flow">
        <http:listener config-ref="listenerConfig" path="202">
            <http:response-builder statusCode="202"/>
        </http:listener>
        <set-payload value="ok" />
    </flow>

    <flow name="203Flow">
        <http:listener config-ref="listenerConfig" path="203">
            <http:response-builder statusCode="203"/>
        </http:listener>
        <set-payload value="ok" />
    </flow>

    <flow name="exceptionFlow">
        <http:listener config-ref="listenerConfig" path="500">
            <http:response-builder statusCode="500"/>
        </http:listener>
        <set-payload value="fail" />
        <test:component throwException="true"/>
    </flow>

    <flow name="echoFlow">
        <http:listener config-ref="listenerConfig" path="echo"/>
        <set-payload value="#[payload]" />
    </flow>

</mule>
