# Defines an object that will be registered on the registry during the application deployment
# Note that the class belongs to the plugin and is NOT exported on the plugin properties.
plugin.echotest=org.foo.EchoTest
