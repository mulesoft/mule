<mule xmlns="http://www.mulesoft.org/schema/mule/core"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:cxf="http://www.mulesoft.org/schema/mule/cxf"
       xmlns:http="http://www.mulesoft.org/schema/mule/http"
       xsi:schemaLocation="
       http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
       http://www.mulesoft.org/schema/mule/cxf http://www.mulesoft.org/schema/mule/cxf/current/mule-cxf.xsd
       http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd
       ">

    <http:listener-config name="listenerConfig" host="localhost" port="${port1}"/>
    <http:request-config name="requestConfig" host="localhost" port="${port1}" />

    <flow name="secureProxy">
        <description>
            Secure Proxy using Spring Security
        </description>
        <http:listener path="/proxy-envelope" config-ref="listenerConfig" allowedMethods="POST" />
        <cxf:proxy-service payload="envelope">
            <cxf:ws-security ref="inboundConfig"/>
        </cxf:proxy-service>
        <cxf:proxy-client payload="envelope">
            <cxf:ws-security ref="outboundConfig"/>
        </cxf:proxy-client>
        <http:request config-ref="requestConfig" path="/echo" method="POST" />
    </flow>

    <flow name="secureProxy2">
        <description>
            Secure Proxy using Spring Security
        </description>
        <http:listener path="/proxy-body" config-ref="listenerConfig" allowedMethods="POST" />
        <cxf:proxy-service>
            <cxf:ws-security ref="inboundConfig"/>
        </cxf:proxy-service>
        <cxf:proxy-client payload="body">
            <cxf:ws-security ref="outboundConfig"/>
        </cxf:proxy-client>
        <http:request config-ref="requestConfig" path="/echo" method="POST" />
    </flow>

    <flow name="echoService">
        <http:listener path="/echo" config-ref="listenerConfig" allowedMethods="POST" />
        <log-component/>
    </flow>
    
</mule>
