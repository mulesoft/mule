<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns:db="http://www.mulesoft.org/schema/mule/db"
      xsi:schemaLocation="http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
            http://www.mulesoft.org/schema/mule/db http://www.mulesoft.org/schema/mule/db/current/mule-db.xsd">

    <flow name="deleteDefaultTarget">
        <inbound-endpoint address="vm://deleteDefaultTarget" exchange-pattern="request-response"/>

        <db:delete config-ref="dbConfig">
            <db:parameterized-query>DELETE FROM PLANET WHERE name = 'Venus'</db:parameterized-query>
        </db:delete>
    </flow>

    <flow name="deleteCustomTarget">
        <inbound-endpoint address="vm://deleteCustomTarget" exchange-pattern="request-response"/>

        <db:delete config-ref="dbConfig" target="#[header:OUTBOUND:updateCount]">
            <db:parameterized-query>DELETE FROM PLANET WHERE name = 'Venus'</db:parameterized-query>
        </db:delete>
    </flow>
</mule>
