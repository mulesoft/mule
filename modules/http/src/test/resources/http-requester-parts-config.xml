<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns:http="http://www.mulesoft.org/schema/mule/http"
      xsi:schemaLocation="
               http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
               http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd">

    <http:listener-config name="listenerConfig" host="localhost" port="${port}"/>
    <http:request-config name="httpRequest" host="localhost" port="${port}"/>

    <flow name="testFlow">
    	<http:listener config-ref="listenerConfig" path="/utf" />
        <set-attachment attachmentName="Z&#252;rich" value="Hello" contentType="text/plain" />
        <http:request config-ref="httpRequest" path="/upload" method="POST">
            <http:request-builder>
                <http:header headerName="Content-Type" value="multipart/form-data"/>
            </http:request-builder>
        </http:request>
    </flow>
    
    <flow name="testRequestFlow">
        <http:listener config-ref="listenerConfig" path="/upload" />
        <set-payload value="#[message.inboundAttachments['Z&#252;rich']]"  />
    </flow>
    
</mule>
