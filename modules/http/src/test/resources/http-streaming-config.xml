<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns:http="http://www.mulesoft.org/schema/mule/http"
      xmlns:vm="http://www.mulesoft.org/schema/mule/vm"
      xsi:schemaLocation="
               http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
               http://www.mulesoft.org/schema/mule/vm http://www.mulesoft.org/schema/mule/vm/current/mule-vm.xsd
               http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd">

    <http:request-config name="requestConfig" host="localhost" port="${httpPort}" />
    <http:listener-config name="listenerConfig" host="localhost" port="${httpPort}" />

    <flow name="client">
        <http:request config-ref="requestConfig" method="GET" path="/" />
    </flow>

    <flow name="server">
        <http:listener path="/" config-ref="listenerConfig"/>
        <custom-processor class="org.mule.module.http.functional.HttpStreamingTestCase$StoppableInputStreamProcessor"/>
    </flow>

</mule>
