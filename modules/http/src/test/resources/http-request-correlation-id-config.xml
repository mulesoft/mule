<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns:http="http://www.mulesoft.org/schema/mule/http"
      xsi:schemaLocation="
               http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
               http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd">

    <http:request-config name="config" host="localhost" port="${httpPort}" />
    <http:listener-config name="listenerConfig" host="localhost" port="${serverPort}" />

    <flow name="listener">
        <http:listener config-ref="listenerConfig" path="/"/>
        <http:request config-ref="config" path="testPath"/>
    </flow>

    <flow name="default">
        <http:request config-ref="config" path="testPath"/>
    </flow>

    <flow name="property">
        <set-property propertyName="MULE_CORRELATION_ID" value="propertyCorrelationId"/>
        <http:request config-ref="config" path="testPath"/>
    </flow>

    <flow name="header">
        <http:request config-ref="config" path="testPath">
            <http:request-builder>
                <http:header headerName="MULE_CORRELATION_ID" value="headerCorrelationId"/>
            </http:request-builder>
        </http:request>
    </flow>

</mule>
