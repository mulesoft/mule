<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns:petstore="http://www.mulesoft.org/schema/mule/petstore"
      xmlns="http://www.mulesoft.org/schema/mule/core"
      xsi:schemaLocation="http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
               http://www.mulesoft.org/schema/mule/petstore http://www.mulesoft.org/schema/mule/petstore/current/mule-petstore.xsd">

    <petstore:config name="petstore-valid" cashierName="jim">
        <petstore:valid-connection username="john" password="doe">
            <reconnection failsDeployment="true">
                <reconnect count="2" frequency="5"/>
            </reconnection>
        </petstore:valid-connection>
        <petstore:pets>
            <petstore:pet value="Lassie"/>
            <petstore:pet value="Sapo Pepe"/>
            <petstore:pet value="My mother's parrot"/>
        </petstore:pets>
    </petstore:config>

    <flow name="fail-paged">
        <petstore:fail-paged-operation failOnPage="#[vars.pageNumber default 0]" stickyConnections="#[vars.sticky default false]" throwConnectivity="#[vars.throwConnectivity default true]" config-ref="petstore-valid"/>
        <foreach>
            <logger message="#[payload]"/>
        </foreach>
    </flow>

    <flow name="fail-paged-error-handler">
        <petstore:fail-paged-operation-with-error-handler errorAction="#[vars.errorAction default 'NONE']" config-ref="petstore-valid"/>
    </flow>

</mule>
