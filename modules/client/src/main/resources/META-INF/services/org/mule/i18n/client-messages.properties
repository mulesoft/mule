1=Failed to dispatch client event
2=There is no event specified in the invoke args of the Client proxy listener. Parameter args[] length is zero
3=A Mule instance is already configured for the JVM. You need to call MuleManager.getInstance().dispose() to\
  destroy the current instance before instanciating a new instance
