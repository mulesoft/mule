<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns:oauth2="http://www.mulesoft.org/schema/mule/oauth2"
      xmlns:http="http://www.mulesoft.org/schema/mule/http"
      xmlns:json="http://www.mulesoft.org/schema/mule/json"
      xsi:schemaLocation="
       http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
       http://www.mulesoft.org/schema/mule/oauth2 http://www.mulesoft.org/schema/mule/oauth2/current/mule-oauth2.xsd
       http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd
       http://www.mulesoft.org/schema/mule/json http://www.mulesoft.org/schema/mule/json/current/mule-json.xsd">

    <oauth2:authorization-code-config name="customConfig"
                                clientId="${client.id}"
                                clientSecret="${client.secret}"
                                redirectionUrl="${redirect.url}">
        <oauth2:custom-token-request
                refreshTokenWhen="#[http.statusCode == 403]"
                tokenUrlCallFlow="oauthTokenAcquicitionFlow"
                refreshTokenFlow="oauthRefreshTokenFlow"/>
    </oauth2:authorization-code-config>

    <flow name="oauthRefreshTokenFlow">
        <logger/>
    </flow>

    <flow name="oauthTokenAcquicitionFlow">
        <expression-component>
            var code = message.inboundProperties['http.query.params']['code'];
            payload = ['code' : code, 'client_id' : '${client.id}', 'client_secret' : '${client.secret}', 'grant_type' :
            'authorization_code', 'redirect_uri' : '${redirect.url}']
        </expression-component>
        <http:request host="${token.host}" port="${token.port}" path="${token.path}" method="POST"/>
        <json:json-to-object-transformer returnClass="java.lang.Object"/>
        <oauth2:store-authorization-code-state config="customConfig"
                                                accessToken="#[payload['access_token']]"
                                                refreshToken="#[payload['refresh_token']]"
                                                expiresIn="#[payload['expires_in']]"/>
    </flow>

</mule>
