<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns:jetty="http://www.mulesoft.org/schema/mule/jetty"
      xmlns:test="http://www.mulesoft.org/schema/mule/test"
      xsi:schemaLocation="
      http://www.mulesoft.org/schema/mule/jetty http://www.mulesoft.org/schema/mule/jetty/current/mule-jetty.xsd
       http://www.mulesoft.org/schema/mule/test http://www.mulesoft.org/schema/mule/test/current/mule-test.xsd
       http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd">


    <jetty:connector name="httpConnector"/>

    <flow name="loopbackAddressEndpointFlow">
        <jetty:inbound-endpoint address="http://127.0.0.1:${port1}/loopbackAddress"
                                exchange-pattern="request-response"/>
        <test:component appendString=" received at loopback address" />
    </flow>

    <flow name="localhostEndpointFlow">
        <jetty:inbound-endpoint address="http://localhost:${port2}/localhost"
                                exchange-pattern="request-response"/>
        <test:component appendString=" received at localhost" />
    </flow>

    <flow name="allAddressesEndpointFlow">
        <jetty:inbound-endpoint address="http://0.0.0.0:${port3}/allAddresses"
                                exchange-pattern="request-response"/>
        <test:component appendString=" received at all addresses" />
    </flow>

</mule>
