<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:quartz="http://www.mulesoft.org/schema/mule/quartz"
      xmlns:vm="http://www.mulesoft.org/schema/mule/vm"
      xmlns:spring="http://www.springframework.org/schema/beans" 
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="
          http://www.mulesoft.org/schema/mule/quartz http://www.mulesoft.org/schema/mule/quartz/current/mule-quartz.xsd
          http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
          http://www.mulesoft.org/schema/mule/vm http://www.mulesoft.org/schema/mule/vm/current/mule-vm.xsd
          http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-current.xsd">

    <spring:beans>
        <spring:bean id="quartzSchedulerFactory" name="quartzSchedulerFactory" class="org.springframework.scheduling.quartz.SchedulerFactoryBean" lazy-init="default">
            <spring:property name="autoStartup" value="true"/>
        </spring:bean>
    </spring:beans>

    <quartz:connector name="Quartz" validateConnections="true" scheduler-ref="quartzSchedulerFactory"/>

    <quartz:endpoint jobName="testJob1" repeatInterval="1" repeatCount="-1" name="testEndpoint" connector-ref="Quartz"/>

    <vm:connector name="vmQueueConnector"/>

    <flow name="quartz-statefulFlow">
        <quartz:inbound-endpoint ref="testEndpoint">
            <quartz:endpoint-polling-job stateful="true">
                <quartz:job-endpoint address="vm://testInput"/>
            </quartz:endpoint-polling-job>
        </quartz:inbound-endpoint>

        <component>
            <singleton-object class="org.mule.transport.quartz.QuartzStatefulEndpointPollingTestCase$BlockingComponent"/>
        </component>
    </flow>
</mule>
