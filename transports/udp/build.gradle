group = 'org.mule.transports'
archivesBaseName = 'mule-transport-udp'
description = 'A Mule transport for Udp Connectivity. This transport supplies a Udp Data Packet dispatcher and client implementation.'

dependencies {
    compile project(':module-spring-config')

    testCompile project(':test-functional'),
        project(":transport-vm")
}
