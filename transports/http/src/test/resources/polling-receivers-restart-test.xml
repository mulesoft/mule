<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mule="http://www.mulesource.org/schema/mule/core/2.0"
       xmlns:http="http://www.mulesource.org/schema/mule/http/2.0"
       xmlns:stdio="http://www.mulesource.org/schema/mule/stdio/2.0"
    xsi:schemaLocation="
       http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-2.0.xsd
       http://www.mulesource.org/schema/mule/core/2.0 http://www.mulesource.org/schema/mule/core/2.0/mule.xsd
       http://www.mulesource.org/schema/mule/stdio/2.0 META-INF/mule-stdio.xsd
       http://www.mulesource.org/schema/mule/http/2.0 http://www.mulesource.org/schema/mule/http/2.0/mule-http.xsd">

    <http:connector name="PollingHttpConnector">
        <mule:service-overrides messageReceiver="org.mule.providers.http.PollingHttpMessageReceiver"/>
    </http:connector>

    <http:connector name="HttpConnector"/>

    <bean name="ftc" class="org.mule.tck.functional.FunctionalTestComponent"/>

    <bean name="static" class="org.mule.components.simple.StaticComponent">
        <property name="data" value="Here's your content."/>
    </bean>

    <mule:model-seda name="http polling test model">

        <!-- singleton for easier testing only -->
        <mule:service name="Test" service-ref="ftc" singleton="true">
            <mule:inbound-router>
                <mule:endpoint name="httpEndpoint" connector-ref="PollingHttpConnector"
                               address="http://localhost:12345/TestServer"/>
            </mule:inbound-router>
            <mule:outbound-router>
                <mule:pass-through-router>
                    <mule:endpoint address="stdio://System.out"/>
                </mule:pass-through-router>
            </mule:outbound-router>
        </mule:service>

        <mule:service name="TestServer" service-ref="static">
            <mule:inbound-router>
                <mule:endpoint address="http://localhost:12345" connector-ref="HttpConnector"/>
            </mule:inbound-router>
        </mule:service>

    </mule:model-seda>

</beans>
