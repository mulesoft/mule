<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns:mule="http://www.mulesoft.org/schema/mule/core"
      xmlns:jms="http://www.mulesoft.org/schema/mule/jms"
      xmlns:vm="http://www.mulesoft.org/schema/mule/vm"
      xmlns:jbossts="http://www.mulesoft.org/schema/mule/jbossts"
      xmlns:test="http://www.mulesoft.org/schema/mule/test"
      xsi:schemaLocation="
       http://www.mulesoft.org/schema/mule/test http://www.mulesoft.org/schema/mule/test/current/mule-test.xsd
       http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
       http://www.mulesoft.org/schema/mule/jms http://www.mulesoft.org/schema/mule/jms/current/mule-jms.xsd
       http://www.mulesoft.org/schema/mule/vm http://www.mulesoft.org/schema/mule/vm/current/mule-vm.xsd
       http://www.mulesoft.org/schema/mule/jbossts http://www.mulesoft.org/schema/mule/jbossts/current/mule-jbossts.xsd">

    <jbossts:transaction-manager/>

    <vm:connector name="vmQueue"/>

    <vm:endpoint name="in" path="in" connector-ref="vmQueue" exchange-pattern="one-way"/>
    <vm:endpoint name="out" path="out" connector-ref="vmQueue" exchange-pattern="one-way"/>

    <flow name="Pre">
        <inbound-endpoint ref="in"/>
        <test:component/>
        <jms:outbound-endpoint queue="middle" exchange-pattern="one-way">
            <xa-transaction action="BEGIN_OR_JOIN"/>
        </jms:outbound-endpoint>
        <!--<xa-transaction action="BEGIN_OR_JOIN"/>-->
    </flow>

    <flow name="Part1">
        <jms:inbound-endpoint queue="middle" exchange-pattern="one-way">
            <xa-transaction action="BEGIN_OR_JOIN"/>
        </jms:inbound-endpoint>
        <test:component/>
        <jms:outbound-endpoint queue="middle2" exchange-pattern="one-way">
            <xa-transaction action="BEGIN_OR_JOIN"/>
        </jms:outbound-endpoint>
    </flow>

    <flow name="Part2">
        <jms:inbound-endpoint queue="middle2" exchange-pattern="one-way">
            <xa-transaction action="BEGIN_OR_JOIN"/>
        </jms:inbound-endpoint>
        <test:component/>
        <jms:outbound-endpoint queue="middle3" exchange-pattern="one-way">
            <xa-transaction action="BEGIN_OR_JOIN"/>
        </jms:outbound-endpoint>
    </flow>

    <flow name="Part3">
        <jms:inbound-endpoint queue="middle3" exchange-pattern="one-way">
            <xa-transaction action="BEGIN_OR_JOIN"/>
        </jms:inbound-endpoint>
        <test:component/>
        <jms:outbound-endpoint queue="q4" exchange-pattern="one-way">
            <xa-transaction action="BEGIN_OR_JOIN"/>
        </jms:outbound-endpoint>
    </flow>

    <flow name="Part4">
        <jms:inbound-endpoint queue="q4" exchange-pattern="one-way">
            <xa-transaction action="BEGIN_OR_JOIN"/>
        </jms:inbound-endpoint>
        <test:component/>
        <jms:outbound-endpoint queue="q5" exchange-pattern="one-way">
            <xa-transaction action="BEGIN_OR_JOIN"/>
        </jms:outbound-endpoint>
        <!--<xa-transaction action="BEGIN_OR_JOIN"/>-->
    </flow>

    <flow name="Outbound">
        <jms:inbound-endpoint queue="q5" exchange-pattern="one-way">
            <xa-transaction action="BEGIN_OR_JOIN"/>
        </jms:inbound-endpoint>
        <test:component>
            <test:return-data>OUTPUT MESSAGE</test:return-data>
        </test:component>
        <outbound-endpoint ref="out">
            <xa-transaction action="NONE"/>
        </outbound-endpoint>
    </flow>

</mule>
