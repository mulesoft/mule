group = 'org.mule.transports'
archivesBaseName = 'mule-transport-xmpp'
description = 'A Mule transport that enables event sending and receiving over XMPP (Jabber).'

dependencies {
    compile project(':module-spring-config'),
        "jivesoftware:smack:3.1.0",
        "jivesoftware:smackx:3.1.0"

    testCompile project(':test-functional'),
        project(":transport-vm")
}
