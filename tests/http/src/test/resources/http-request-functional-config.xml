<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns:httpn="http://www.mulesoft.org/schema/mule/httpn"
      xsi:schemaLocation="
               http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
               http://www.mulesoft.org/schema/mule/httpn http://www.mulesoft.org/schema/mule/httpn/current/mule-httpn.xsd">

    <httpn:request-config name="requestConfig" basePath="basePath">
        <httpn:request-connection host="localhost" port="${httpPort}"/>
    </httpn:request-config>

    <httpn:request-config name="requestConfigHttp">
        <httpn:request-connection host="localhost"/>
    </httpn:request-config>


    <httpn:request-config name="requestConfigHttps">
        <httpn:request-connection protocol="HTTPS" host="localhost"/>
    </httpn:request-config>

    <flow name="requestFlow">
        <httpn:request config-ref="requestConfig" method="POST" path="requestPath"/>
        <object-to-string-transformer />
    </flow>

    <!-- MULE-8956 -->
    <httpn:request-config name="blockingRequestConfig" basePath="basePath">
        <httpn:request-connection host="localhost" port="${blockingHttpPort}"/>
    </httpn:request-config>
    <httpn:listener-config name="blockingListenerConfig" basePath="basePath">
        <httpn:listener-connection host="localhost" port="${blockingHttpPort}"/>
    </httpn:listener-config>

    <flow name="blockingRequestFlow">
        <flow-ref name="blockingRequest"/>
    </flow>
    <flow name="remote">
        <httpn:listener config-ref="blockingListenerConfig" path="/remote" allowedMethods="GET"/>
        <set-payload value="#[%output application/xml --- { root: {node: 'value'} } ]"/>
    </flow>
    <flow name="blockingRequest">
        <httpn:request config-ref="blockingRequestConfig" path="remote" method="GET"/>
        <set-payload value="#[%output application/java --- payload.root.node]"/>
        <object-to-string-transformer />
    </flow>

</mule>
