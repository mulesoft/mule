<mule xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns:vm="http://www.mulesoft.org/schema/mule/vm"

      xsi:schemaLocation="
          http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
          http://www.mulesoft.org/schema/mule/vm http://www.mulesoft.org/schema/mule/vm/current/mule-vm.xsd">

    <model name="testModel">
        <service name="Service1">
            <inbound>
                <vm:inbound-endpoint path="Service1.in"/>
                <collection-aggregator-router timeout="60000" failOnTimeout="false"/>
            </inbound>
        </service>

        <service name="Service2">
            <inbound>
                <vm:inbound-endpoint path="Service2.in"/>
                <collection-aggregator-router timeout="60000" failOnTimeout="false"/>
            </inbound>
        </service>

        <service name="Service3">
            <inbound>
                <vm:inbound-endpoint path="Service3.in"/>
                <collection-aggregator-router timeout="60000" failOnTimeout="false"/>
            </inbound>
        </service>

        <service name="Service4">
            <inbound>
                <vm:inbound-endpoint path="Service4.in"/>
                <collection-aggregator-router timeout="60000" failOnTimeout="false"/>
            </inbound>
        </service>

        <service name="Service5">
            <inbound>
                <vm:inbound-endpoint path="Service5.in"/>
                <collection-aggregator-router timeout="60000" failOnTimeout="false"/>
            </inbound>
        </service>

        <service name="Service6">
            <inbound>
                <vm:inbound-endpoint path="Service6.in"/>
                <collection-aggregator-router timeout="60000" failOnTimeout="false"/>
            </inbound>
        </service>

        <service name="Service7">
            <inbound>
                <vm:inbound-endpoint path="Service7.in"/>
                <collection-aggregator-router timeout="60000" failOnTimeout="false"/>
            </inbound>
        </service>

        <service name="Service8">
            <inbound>
                <vm:inbound-endpoint path="Service8.in"/>
                <collection-aggregator-router timeout="60000" failOnTimeout="false"/>
            </inbound>
        </service>

        <service name="Service9">
            <inbound>
                <vm:inbound-endpoint path="Service9.in"/>
                <collection-aggregator-router timeout="60000" failOnTimeout="false"/>
            </inbound>
            <threading-profile maxBufferSize="5" maxThreadsActive="20" maxThreadsIdle="5" poolExhaustedAction="RUN" threadTTL="500" threadWaitTimeout="500"/>
        </service>
        <service name="Service10">
            <inbound>
                <vm:inbound-endpoint path="Service10.in"/>
                <collection-aggregator-router timeout="60000" failOnTimeout="false"/>
            </inbound>
        </service>

        <service name="Service11">
            <inbound>
                <vm:inbound-endpoint path="Service11.in"/>
                <collection-aggregator-router timeout="60000" failOnTimeout="false"/>
            </inbound>
        </service>

        <service name="Service12">
            <inbound>
                <vm:inbound-endpoint path="Service12.in"/>
                <collection-aggregator-router timeout="60000" failOnTimeout="false"/>
            </inbound>
        </service>

        <service name="Service13">
            <inbound>
                <vm:inbound-endpoint path="Service13.in"/>
                <collection-aggregator-router timeout="60000" failOnTimeout="false"/>
            </inbound>
        </service>

        <service name="Service14">
            <inbound>
                <vm:inbound-endpoint path="Service14.in"/>
                <collection-aggregator-router timeout="60000" failOnTimeout="false"/>
            </inbound>
        </service>

        <service name="Service15">
            <inbound>
                <vm:inbound-endpoint path="Service15.in"/>
                <collection-aggregator-router timeout="60000" failOnTimeout="false"/>
            </inbound>
        </service>

        <service name="Service16">
            <inbound>
                <vm:inbound-endpoint path="Service16.in"/>
                <collection-aggregator-router timeout="60000" failOnTimeout="false"/>
            </inbound>
        </service>
    </model>
</mule>