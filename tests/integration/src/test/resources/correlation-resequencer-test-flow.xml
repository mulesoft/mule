<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesoft.org/schema/mule/core" 
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns:spring="http://www.springframework.org/schema/beans" 
      xmlns:test="http://www.mulesoft.org/schema/mule/test"
      xmlns:vm="http://www.mulesoft.org/schema/mule/vm"
      xsi:schemaLocation="
               http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-current.xsd
               http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
               http://www.mulesoft.org/schema/mule/vm http://www.mulesoft.org/schema/mule/vm/current/mule-vm.xsd
               http://www.mulesoft.org/schema/mule/test http://www.mulesoft.org/schema/mule/test/current/mule-test.xsd">


    <!-- Split the message and set correlation group ids and sizes -->
    <flow name="splitter service">
        <vm:inbound-endpoint path="splitter" exchange-pattern="one-way"/>
        <collection-splitter/>
        <vm:outbound-endpoint path="resequencer" exchange-pattern="one-way"/>
    </flow>

    <flow name="resequencer service">
        <vm:inbound-endpoint path="resequencer" exchange-pattern="one-way"/>
        <resequencer/>
        <vm:outbound-endpoint path="sorted" exchange-pattern="request-response"/>
    </flow>

    <flow name="test validator">
        <vm:inbound-endpoint path="sorted" exchange-pattern="request-response"/>
        <test:component/>
    </flow>

</mule>
