<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesoft.org/schema/mule/core"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:spring="http://www.springframework.org/schema/beans"
       xmlns:test="http://www.mulesoft.org/schema/mule/test"
       xmlns:jms="http://www.mulesoft.org/schema/mule/jms"
       xsi:schemaLocation="
       http://www.mulesoft.org/schema/mule/test http://www.mulesoft.org/schema/mule/test/current/mule-test.xsd
       http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-current.xsd
       http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
       http://www.mulesoft.org/schema/mule/jms http://www.mulesoft.org/schema/mule/jms/current/mule-jms.xsd">
       
   <jms:activemq-connector name="jmsConnector"/>

   <model name="ChainingRouterRemoteSync">

      <service name="Part2Service" initialState="started">
         <inbound>
            <inbound-endpoint address="vm://in" exchange-pattern="request-response"/>
         </inbound>
         <outbound>
            <custom-outbound-router class="org.mule.routing.outbound.ChainingRouter">
               <outbound-endpoint address="jms://out1" exchange-pattern="request-response"/>
               <outbound-endpoint address="vm://out2" exchange-pattern="request-response"/>
            </custom-outbound-router>
         </outbound>
      </service>
      
      <service name="Part2ServiceLastEndpointDispatch" initialState="started">
         <inbound>
            <inbound-endpoint address="vm://in2" exchange-pattern="request-response"/>
         </inbound>
         <outbound>
            <custom-outbound-router class="org.mule.routing.outbound.ChainingRouter">
               <outbound-endpoint address="jms://out1" exchange-pattern="request-response"/>
               <outbound-endpoint address="vm://out2" exchange-pattern="request-response"/>
               <outbound-endpoint address="jms://out2" exchange-pattern="one-way"/>
            </custom-outbound-router>
         </outbound>
      </service>

      <service name="Part3Service" initialState="started">
         <inbound>
            <inbound-endpoint address="jms://out1" exchange-pattern="request-response"/>
         </inbound>
         <test:component appendString=" [REMOTESYNC RESPONSE]"/>
      </service>

      <service name="Part4Service" initialState="started">
         <inbound>
            <inbound-endpoint address="vm://out2" exchange-pattern="request-response"/>
         </inbound>
         <test:component appendString=" [REMOTESYNC RESPONSE 2]"/>
      </service>
            
    </model>

</mule>
