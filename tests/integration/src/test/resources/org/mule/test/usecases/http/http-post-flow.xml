<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesoft.org/schema/mule/core" 
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns:spring="http://www.springframework.org/schema/beans" 
      xmlns:http="http://www.mulesoft.org/schema/mule/http"
      xsi:schemaLocation="
       http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-current.xsd
       http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
       http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd">

    <configuration>
        <http:config useTransportForUris="true"/>
    </configuration>

    <http:endpoint name="httpRequest" address="http://localhost:${port}/RemoteService" exchange-pattern="request-response"
                   method="POST">
        <property key="http.post.body.param" value="IncidentData"/>
    </http:endpoint>

    <endpoint name="httpService" address="http://localhost:${port}/RemoteService" exchange-pattern="request-response"/>

    <flow name="HttpService">
        <inbound-endpoint ref="httpService"/>
        <echo-component/>
    </flow>

</mule>
