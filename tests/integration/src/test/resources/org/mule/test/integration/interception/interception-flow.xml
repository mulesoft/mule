<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns:test="http://www.mulesoft.org/schema/mule/test"
      xsi:schemaLocation="
               http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
               http://www.mulesoft.org/schema/mule/test http://www.mulesoft.org/schema/mule/test/current/mule-test.xsd">

    <flow name="setPayloadFlow">
        <set-payload value="#[inputMessage]"/>
    </flow>

    <flow name="failingProcessorFlow">
        <custom-processor class="org.mule.test.integration.interception.MessageProcessorInterceptionFlowTestCase$FailingProcessor"/>
    </flow>

    <flow name="countingProcessorFlow">
        <custom-processor class="org.mule.test.integration.interception.MessageProcessorInterceptionFlowTestCase$CountingProcessor"/>
    </flow>

    <flow name="internalProcessorFlow">
        <custom-processor class="org.mule.test.integration.interception.MessageProcessorInterceptionFlowTestCase$InternalProcessor"/>
    </flow>

    <flow name="flowWithInnerFlow">
        <flow-ref name="innerFlow"/>
    </flow>
    <flow name="innerFlow">
        <set-payload value="#[inputMessage]"/>
    </flow>

    <flow name="routerFlow">
        <choice>
            <when expression="inputMessage != null">
                <set-payload value="#[inputMessage]"/>
            </when>
            <otherwise>
                <set-payload value=""/>
            </otherwise>
        </choice>
    </flow>

    <flow name="interceptingProcessorFlow">
        <splitter expression="#[payload.fruit]"/>
        <set-payload value="#[inputMessage]"/>
    </flow>
    
    <flow name="pollingFlow" initialState="stopped">
        <poll frequency="100">
            <set-payload value="#[inputMessage]"/>
        </poll>
        <custom-processor class="org.mule.test.integration.interception.MessageProcessorInterceptionFlowTestCase$InternalProcessor"/>
    </flow>
    
</mule>
