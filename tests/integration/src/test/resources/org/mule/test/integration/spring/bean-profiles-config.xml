<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns:spring="http://www.springframework.org/schema/beans"
      xsi:schemaLocation="
      http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-current.xsd
      http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd">

	<spring:beans profile="one">
		<mule>
		    <append-string-transformer message="Manzi" name="append"/>
		</mule>
	</spring:beans>

	<spring:beans profile="two">
		<mule>
		    <append-string-transformer message="Simpson" name="append"/>
		</mule>
	</spring:beans>

    <flow name="service">
        <inbound-endpoint address="vm://in">
        </inbound-endpoint>
        <transformer ref="append"/>
        <outbound-endpoint address="vm://out"/>
    </flow>   

</mule>
