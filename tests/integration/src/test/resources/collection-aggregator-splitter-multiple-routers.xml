<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns:vm="http://www.mulesoft.org/schema/mule/vm"
      xsi:schemaLocation="
               http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
               http://www.mulesoft.org/schema/mule/vm http://www.mulesoft.org/schema/mule/vm/current/mule-vm.xsd">

    <vm:connector name="queue"/>

    <flow name="multiple-splitter-aggregator-payload-preserved">
        <collection-splitter/>
        <collection-aggregator/>
        <collection-splitter/>
        <collection-aggregator/>
        <vm:outbound-endpoint path="out" exchange-pattern="one-way" connector-ref="queue"/>

    </flow>

    <flow name="multiple-splitter-aggregator-successive-payload-transformations">
        <collection-splitter/>
        <set-payload value="#[payload * 2]"/>
        <collection-aggregator timeout="2000" failOnTimeout="false" persistentStores="false"/>
        <collection-splitter/>
        <set-payload value="#[payload * 3]"/>
        <collection-aggregator timeout="2000" failOnTimeout="false" persistentStores="false"/>
        <collection-splitter/>
        <set-payload value="#[payload * 4]"/>
        <collection-aggregator timeout="2000" failOnTimeout="false" persistentStores="false"/>
        <vm:outbound-endpoint path="out" exchange-pattern="one-way" connector-ref="queue"/>
    </flow>

</mule>
