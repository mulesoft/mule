<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesource.org/schema/mule/core/2.0"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:spring="http://www.springframework.org/schema/beans"
       xmlns:vm="http://www.mulesource.org/schema/mule/vm/2.0"
       xsi:schemaLocation="
               http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-2.0.xsd
               http://www.mulesource.org/schema/mule/core/2.0 http://www.mulesource.org/schema/mule/core/2.0/mule.xsd
               http://www.mulesource.org/schema/mule/vm/2.0 http://www.mulesource.org/schema/mule/vm/2.0/mule-vm.xsd">

    <notifications>
        <!-- the test event is routed to the test interface -->
        <route event-class="org.mule.config.spring.parsers.specific.ServerNotificationManagerTestCase$TestEvent"
               interface="org.mule.config.spring.parsers.specific.ServerNotificationManagerTestCase$TestInterface"/>

        <!-- adming events are routed to the test interface 2, but disabled for the usual admin listener -->
        <route event="ADMIN"
               interface="org.mule.config.spring.parsers.specific.ServerNotificationManagerTestCase$TestInterface2"/>
        <disable interface="org.mule.impl.internal.notifications.AdminNotificationListener"/>

        <!-- this receives the simple test event -->
        <listener ref="listener"/>

        <!-- the admin event should go to the test interface 2, but not to the disabled adming interface -->
        <listener ref="listener2"/>
        <listener ref="adminListener"/>
    </notifications>

    <spring:bean name="listener"
                 class="org.mule.config.spring.parsers.specific.ServerNotificationManagerTestCase$TestListener"/>
    <spring:bean name="listener2"
                 class="org.mule.config.spring.parsers.specific.ServerNotificationManagerTestCase$TestListener2"/>
    <spring:bean name="adminListener"
                 class="org.mule.config.spring.parsers.specific.ServerNotificationManagerTestCase$TestAdminListener"/>

</mule>
